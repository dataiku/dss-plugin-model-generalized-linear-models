import dataiku
import os
from dataiku.customwebapp import get_webapp_config
from dku_config.dku_model_handler import ModelHandler

# class GLM_Handler:
#     """GLM_Handler: A class to facilitate the connection to GLM"""

#     def __init__(self):
#         webapp_config = get_webapp_config()
#         saved_model_id = webapp_config['saved_model_id']
#         self.model_handler = ModelHandler(saved_model_id)

# glm_handler = GLM_Handler()
